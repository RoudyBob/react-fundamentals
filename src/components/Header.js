// import React from 'react'
// only need to import if using class components

// You can do it as a standard named function.
// function Header() {

// };

// Or you can do it as a fat arrow function as an expression.
const Header = () => {
    return (
        <h5>My first functional component.</h5>
    );
};

export default Header;